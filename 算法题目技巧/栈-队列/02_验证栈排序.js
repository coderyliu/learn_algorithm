// https://leetcode.cn/problems/validate-stack-sequences/
/**
 * @param {number[]} pushed
 * @param {number[]} popped
 * @return {boolean}
 */
var validateStackSequences = function (pushed, popped) {
  // for (let i = 0; i < pushed.length; i++) {
  //   if (i === 0 && popped.includes(pushed[i])) continue
  //   const 
  // }
};